---
const navLinks = [
  { href: '/', label: 'Etusivu' },
  { href: '/carl', label: 'Carl' },
  { href: '/tavoitteeni', label: 'Tavoitteeni' },
  { href: '/blogi', label: 'Blogi / Ajankohtaista' },
  { href: '/ota-yhteytta', label: 'Yhteystiedot' },
];

const currentPath = Astro.url.pathname;

function isActive(href: string): boolean {
  if (href === '/') return currentPath === '/';
  return currentPath.startsWith(href);
}
---
<header class="site-header">
  <div class="container-custom header-inner">

    <!-- Logo: large offwhite version for transparent state, small navy version for scrolled state -->
    <a href="/" aria-label="Carl Willandt — etusivu" style="flex-shrink: 0; text-decoration: none; display: flex; align-items: center;">
      <img src="/carl-willandt-logo.svg" alt="Carl Willandt" class="header-logo logo-large" />
      <img src="/carl-willandt-logo-small.svg" alt="Carl Willandt" class="header-logo logo-small" />
    </a>

    <!-- Desktop nav -->
    <nav class="desktop-nav" aria-label="Päänavigaatio">
      {navLinks.map(link => (
        <a
          href={link.href}
          class={`nav-link${isActive(link.href) ? ' nav-link--active' : ''}`}
        >
          {link.label}
        </a>
      ))}
      <a href="/vapaaehtoiseksi" class="btn-primary nav-cta">
        Tukijoukot
      </a>
    </nav>

    <!-- Mobile hamburger -->
    <button
      id="menu-toggle"
      aria-label="Avaa valikko"
      aria-expanded="false"
      aria-controls="mobile-menu"
      class="mobile-toggle"
    >
      <svg id="icon-hamburger" width="24" height="24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" viewBox="0 0 24 24">
        <line x1="4" y1="6" x2="20" y2="6"/><line x1="4" y1="12" x2="20" y2="12"/><line x1="4" y1="18" x2="20" y2="18"/>
      </svg>
      <svg id="icon-close" width="24" height="24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" viewBox="0 0 24 24" style="display: none;">
        <line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/>
      </svg>
    </button>
  </div>

  <!-- Mobile menu -->
  <div id="mobile-menu" class="mobile-menu" style="display: none;">
    <nav class="container-custom mobile-nav" aria-label="Mobiilinavigaatio">
      {navLinks.map(link => (
        <a
          href={link.href}
          class={`mobile-nav-link${isActive(link.href) ? ' mobile-nav-link--active' : ''}`}
        >
          {link.label}
        </a>
      ))}
      <a href="/vapaaehtoiseksi" class="btn-primary" style="text-align: center; margin-top: 1rem; font-size: 0.875rem;">
        Tukijoukot
      </a>
    </nav>
  </div>
</header>

<style>
  .site-header {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 50;
    background-color: transparent;
    transition: background-color 0.25s, border-color 0.25s;
    border-bottom: 1px solid transparent;
  }

  .site-header.scrolled {
    background-color: var(--color-offwhite);
    border-bottom-color: #ffffff;
  }

  .header-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding-top: 0.75rem;
    padding-bottom: 0.75rem;
  }

  /* Logo variants */
  .logo-large {
    height: 3rem;
    width: auto;
    display: block;
  }

  .logo-small {
    display: none;
  }

  .site-header.scrolled .logo-large {
    display: none;
  }

  .site-header.scrolled .logo-small {
    display: block;
    height: 2.25rem;
    width: auto;
  }

  /* Desktop nav */
  .desktop-nav {
    display: none;
    align-items: center;
    gap: 0;
  }

  .nav-link {
    display: flex;
    align-items: center;
    padding: 1.05rem 1.3125rem;
    font-size: 1rem;
    font-weight: 600;
    letter-spacing: 0;
    text-transform: uppercase;
    text-decoration: none;
    color: var(--color-offwhite);
    white-space: nowrap;
    border-bottom: 5px solid transparent;
    transition: border-color 0.15s, color 0.15s;
    box-sizing: border-box;
  }

  .nav-link:hover {
    border-bottom-color: rgba(78, 138, 91, 0.45);
  }

  .nav-link--active {
    border-bottom-color: var(--color-green-brand);
  }

  /* Text colour flips to navy when header has background */
  .site-header.scrolled .nav-link {
    color: var(--color-navy);
  }

  .nav-cta {
    margin-left: 1.3125rem;
    font-size: 1rem;
    letter-spacing: 0;
    flex-shrink: 0;
  }

  /* Mobile */
  .mobile-toggle {
    background: none;
    border: none;
    cursor: pointer;
    padding: 0.5rem;
    color: var(--color-offwhite);
    transition: color 0.15s;
  }

  .site-header.scrolled .mobile-toggle {
    color: var(--color-navy);
  }

  .mobile-menu {
    background-color: var(--color-navy-light);
    border-top: 1px solid rgba(255,255,255,0.08);
  }

  .mobile-nav {
    padding-top: 0.75rem;
    padding-bottom: 1rem;
    display: flex;
    flex-direction: column;
  }

  .mobile-nav-link {
    color: var(--color-offwhite);
    font-weight: 600;
    font-size: 0.9375rem;
    letter-spacing: 0;
    padding: 0.75rem 0;
    border-bottom: 1px solid rgba(255,255,255,0.06);
    text-decoration: none;
    border-left: 3px solid transparent;
    transition: border-left-color 0.15s;
  }

  .mobile-nav-link--active {
    border-left-color: var(--color-green-brand);
    padding-left: 0.75rem;
  }

  @media (min-width: 1280px) {
    .desktop-nav { display: flex !important; }
    .mobile-toggle { display: none !important; }

    /* Transparent desktop: 266px-wide logo (111px tall) + 1.5rem top + 1.5rem bottom = 9.9375rem */
    .header-inner {
      padding-top: 1.5rem;
      padding-bottom: 1.5rem;
    }

    .site-header.scrolled .header-inner {
      padding-top: 1.25rem;
      padding-bottom: 1.25rem;
    }

    .logo-large {
      width: 266px;
      height: auto;
    }

    .site-header.scrolled .logo-small {
      height: 2.75rem;
      width: auto;
    }
  }
</style>

<script>
  const toggle = document.getElementById('menu-toggle')!;
  const menu = document.getElementById('mobile-menu')!;
  const iconHamburger = document.getElementById('icon-hamburger')!;
  const iconClose = document.getElementById('icon-close')!;
  toggle.addEventListener('click', () => {
    const isOpen = menu.style.display !== 'none';
    menu.style.display = isOpen ? 'none' : 'block';
    toggle.setAttribute('aria-expanded', String(!isOpen));
    iconHamburger.style.display = isOpen ? 'block' : 'none';
    iconClose.style.display = isOpen ? 'none' : 'block';
  });

  const header = document.querySelector('.site-header') as HTMLElement;
  const onScroll = () => {
    header.classList.toggle('scrolled', window.scrollY > 60);
  };
  window.addEventListener('scroll', onScroll, { passive: true });
  onScroll();
</script>
